# 0110: explicit-guess value ran cleanly, monitor still has no signal

Boot8d5c055e-b2f0-4195-bdfe-b224236fc09a. User confirms "nothing happens" --
same no-signal outcome as0102-0109. eDP stayed connected throughout.
Private captures:0110-right-connected.txt,0110-right-kernel.log,
0110-dpin-counters.txt under captures/ (not committed).

## Sequence

Identical to0105-0109: route selected with gates deferred,native DPIN0
active handshake completes ("native DPIN0: active=1 handshake=0" -- the
guessed-value writes to+0x14/+0x1c ran alongside the existing HPD/CONTROL
CONNECTED writes and returned success, no error), SET_LINK_RATE0xa, native
crossbar bring-up result0, SET_ACTIVE_LANE_COUNT4, DP IN DPRX_DONE=1, USB4
frame complete id=2. DP-IN packet counter again nonzero (0x35c1).

## What this establishes

This is a clean negative result for the SPECIFIC numeric guess (w20=9),
not new evidence against the underlying formula (independently confirmed
three times over in the prior static-analysis workflow) or against the
general idea that native writes something meaningful at these offsets. As
documented in notes/2026-09-22-0110-mode-value-guess.md before this test,
the weaker of the two guessed subfields (the "secondary bit") was always
the more likely place for the estimate to be wrong; the rate-class half
(2, matching the standard HBR2 ordinal already used elsewhere in this
driver) remains comparatively well-motivated. This result does not
distinguish between "the guess was numerically wrong" and "these two
writes are not the missing piece at all" -- both remain open.

Combined with0106-0109, five separate hypotheses at the USB4/tunnel/DPIN0
level have now been tested with hard evidence (four ruled out cleanly; this
fifth is an inconclusive negative on an explicitly-labeled guess, not a
clean rule-out of the underlying mechanism). Per the plan agreed before
this test, the next step is pursuing the actual DCP coprocessor firmware
(a separate binary from the XNU kernelcache used for all analysis so far),
since the static-analysis workflow concluded the true value most likely
originates there.

No speculative register write beyond what was logged before this
attachment; no reboot performed. Future0110 boot options remain disarmed.
Unplug requested; see follow-up ACTION-LOG entry once confirmed.
