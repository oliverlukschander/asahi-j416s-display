# PHY 3 engine + DPIN analog — 2026-09-21

Hyprland: eDP-1 only. No `device == NULL`.

```
289c00000.dcp  (HDMI hybrid dcpext0)
Switched dpin0 to dispext0,0 (t602x atc=0x1 mux=0x0)
allocated Type-C DPTX PHY 3 (USB4 DP IN)
validate target=0x9030 core=0 atc=3 die=0
powering nub 0x854670
AppleDCPPS190: matched PS190: OUI 00.10.FA, DeviceID pHDMIg
INACTIVE_SINK
SET_LINK_RATE 0x0
tunnel kept, HPD=1 DPRX=0
```

Firmware found a DPTX object (PHY 3) and trained the Mac’s HDMI analog
retimer (Parade PS190 / `pHDMIg`), the empty HDMI jack. DPIN bits 13:12
did not move AUX onto USB-C DP IN. Display-crossbar dpin0 and HDMI PHY 3
are separate analog paths; remote-port `atc=3` binds AUX to the jack.

ATC 2 in USB4 has no firmware DPTX object. PHY 3 has one and it is HDMI.
Next is ACIO-side AUX into DP IN `0:5`, not another DCP/PHY target.
