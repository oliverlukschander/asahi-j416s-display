# 0082 USB4 0x9001 + DCP verbose logs

0081: ATC AUX on `0x9001` ACTIVATEd, analog `+0x18=0x17`, no
`SET_LINK_RATE`. Drop ATC AUX (0079 analog `0x1017`). Raise
`gAFKConfigLogMask` on dcpext1 before the 0x9001 bind so
`AppleDCPDPTX.cpp` after the nub is in dmesg.

Keep skip lpdptxphy, unit 0, unk `0x100`, GET_SUPPORTS_HPD=1,
set_hpd before request_display.

Load with hub unplugged; plug USB-C Right after eDP-1 is up.

Watch for: `DCP verbose logs`, `AppleDCPDPTX`, `SET_LINK_RATE`,
analog `+0x18`, DP IN `DPRX`.

## Boot (hub after eDP)

eDP-1 stayed on. Hub `0-1` `0x5a:0xde81`. VMM7100 USB up. Tunnel
`0:5 ↔ 1:19` VE=AE=HPD=1 DPRX=0. Hyprland eDP-1 only.

```
skip lpdptxphy instantiate
DCP verbose logs ret=0 retcode=0
bind port=0 unit 0
validate/connect unk=0x100/set_hpd 0
AppleDCPDPTX.cpp:355 powering nub 0x854e10
PmgrService.cpp:147 pmgrSetPowerState devID=0x25 devMode=0xF
PmgrService.cpp:301 pmgrResetDevice devID=0x25
GET_SUPPORTS_HPD=1, APCALL 10, APCALL 0 ACTIVATE
request_display 0
```

No `SET_LINK_RATE`. Analog `+0x18=0x17`. Firmware opens/closes
`AppleDCPDPTXRemotePort` around each AFK call, powers DCP-PMGR
device `0x25`, ACTIVATEs, then goes silent. No analog DPTX object
behind that nub.

Closed as a video path. Verbose logs stay useful. Live policy 0079
handshake. Hub-from-init can still blank eDP (`pmgrResetDevice 0x25`
during `request_display`); plug only after the panel is up.
