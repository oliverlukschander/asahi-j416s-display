# 0103: no picture, repeated completion unacknowledged;0104 prepared

Bootfad74e21-836b-45f1-9488-6b5f75a7f4be. User reports no external picture.
eDP remains active. First right-port connection:
301.108145s tunnel clock rate0xa result0;
301.109833s native DPIN0 crossbar bring-up result0,+800=4;
301.110496s link-config up result0;
301.119058s active lanes4;
301.527482s repeated SET_LINK_RATE0xa, cached PHY success;
301.528450s second APCALL6(DID_CHANGE).
301.545396s DPRX_DONE1. No completed external frame captured.

The second DID_CHANGE hits usb4_link_up_attempted and returns-EALREADY
by code inspection; this return was not logged directly. afk.c service
NOTIFY dispatch frees the reply and returns on any nonzero callback result,
so this guard prevents an ACK from being sent. SET_LINK_RATE already treats
an identical active rate as idempotent; the completion must do so too.

At307.207s hub/USB disconnect;342.864s right USB4 reconnect. The first
connection therefore ends about6s after rate setup; reconnection is about
42s after it. User was asked whether this was manual or spontaneous;
Oliver confirms this was manual unplug/replug. An earlier question loosely described the whole event as
40s after connection; these are the precise timestamps. Do not assume cause.
310.240724s modeset timed out.312.733503s rate0 clock cleanup returns0.
Second connection has no new DCP probe because existing experiment is
one-attempt; DPRX times out there. Do not use second-connection state as
an independent first-connection test. No repeated live probe requested.

## 0104 software-only callback correction

Kernel commit fa3f3dd; patch0104. Cache successful link-up rate only after
native bring-up succeeds. With tunnel-clock option and saved clock PHY,
repeat DID_CHANGE at that same nonzero rate returns success after the
existing delay, without invoking crossbar/ACIO helpers again. Failed first
attempts, differing rates and invalidated clock state still return-EALREADY.
Clear cached success on successful rate0 and DEACTIVATE. Attempt latch is
not reset; no new hardware retry/replug capability introduced. Legacy probe
and direct-display behavior unchanged. No new register read/write/mapping.

Actual DID_CHANGE callback compiled with counted mock hardware boundary:
first success,10 identical completions with only1 hardware call, changed
rate/no clock/invalidated cache refusals, failed first attempt never cached,
zero rate and direct/legacy behavior pass with UBSan. This tests callback
logic, not video output. checkpatch0errors0warnings; DRM builds (existing
unused-function and pahole warnings). Installer hashes/nine targets verified.

New appledrm SHA256
9894035809e17b72d82d9f823d40bf115621539bebc74a5b7448f21f31f392e3.
ATC/mux/TB unchanged from0103. manage-0104.py uses distinct config/backup.
Candidate is NOT installed. Future boots remain disarmed; hub currently
connected after the recorded second attachment. No further live hardware
action during this investigation. Manual reconnection confirmed; next test needs a fresh boot and one
connection left in place until capture is complete. Keyboard unverified.
