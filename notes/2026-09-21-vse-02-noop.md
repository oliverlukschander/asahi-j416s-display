# VSE +0x02 is scratch — 2026-09-21 0061

NHI stayed healthy: `CS0=c0090400`. `+0x02 wrote 1 read 00000001`.
DPRX stayed 0. Analog `+0x18=0x17`.

VSE +0x02 is writable scratch next to cable_info, not an AUX enable.
+0x03–0x0a are the same (0059). +0x0b hung NHI. Do not write those.

## 0062

Video hop 9 still has `credits=0` (USB4 field often ignored). Set it
to 7 after the tunnel is up.
