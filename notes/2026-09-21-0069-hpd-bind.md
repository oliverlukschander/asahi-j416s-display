# 0069 HPD bind — analog 0x1016

validate/connect/set_hpd all 0. No request_display. Analog timeout
`+0x18=0x1016` (bit 0 clear, bit 12 set). DPRX=0. eDP on.

0070: connect with `supports_hpd=true` (unk BIT(8)) instead of false
then set_hpd. Firmware may need HPD capability on the remote port
from connect time.
