# 0081 USB4 0x9001 + ATC AUX without lane switch

0080 connect unk `0x101` accepted, analog `+0x18=0x16`, no
`SET_LINK_RATE`. Revert to 0079 unk `0x100`.

0021 enabled Type-C ATC `lpdptx` AUX without switching USB4 SS lanes,
but the remote-port was ATC 2 (`0x8020`/`0x9020`) so firmware logged
`device == NULL`. 0079 `0x9001` ACTIVATEs a CORE=1 nub with no AUX
backend.

0081: keep skip lpdptxphy (eDP). Bind `0x9001` unit 0, unk `0x100`,
GET_SUPPORTS_HPD=1, set_hpd before request_display. Before that bind,
`phy_set_mode_ext(DP)` on the USB-C ATC DP PHY so
`atcphy_dpphy_set_mode` enables AUX only (USB4 mode, no lane switch).
ACTIVATE uses that PHY as `atcphy`.

Load with hub unplugged; plug USB-C Right after eDP-1 is up.

Watch for: `ATC DP AUX without lane switch: 0`, connect unk `0x100`,
`SET_LINK_RATE`, `SET_ACTIVE_LANE_COUNT`, DP IN `DPRX=1`.
