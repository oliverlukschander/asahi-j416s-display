# 0113: dpin_mode_value=0 (corrected formula, CORE=1) ran for real, no picture

Boot 393854ae-59a9-4c26-abaa-ad5dc20ce507. dpin_mode_value=0 (the
formula-corrected, highest-confidence value; see
2026-09-23-mode-value-formula-correction.md). This is the genuine retest
of the attempt invalidated by the 0112 dpin_attempted latch bug (fixed
in 0113). User confirms no picture on the external monitor. eDP-1
remained connected throughout. Private captures:
0113-mv0-right-connected.txt, 0113-mv0-right-kernel.log,
0113-mv0-dpin-counters.txt under captures/ (not committed).

## Sequence

Unlike the invalidated 0112 mv0 attempt, this time the kernel log shows
the native DPIN0 handshake actually ran: "native DPIN0: active=1
base=0x0000000f01e50000, ... mode_value=0" -> "handshake=0" -> crossbar
bring-up result=0 -> link-config up rate=0xa result=0 -> DP IN
DPRX_DONE=1 (ACIO AUX completed). This confirms the 0113 fix works: the
0113 install/boot cycle produced a real, valid test this time.

## What this establishes

The formula-corrected, highest-confidence candidate (mode_value=0,
CORE=1) is now a genuine clean negative, on the same footing as every
other candidate tested since 0102. The remaining candidate from the
corrected formula is mode_value=1 (CORE=2 case), planned next via the
same reboot-free mechanism, now confirmed to actually work end-to-end.
