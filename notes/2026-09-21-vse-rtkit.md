# Apple VSE + no RTKit client IPC — 2026-09-21 0058 boot

Hyprland: eDP-1 only. DPRX=0. No `ACIO RTKit ep=` lines: firmware
sends no client IPC (endpoints likely never started).

## Apple VSE cap=53

```
00=ca0005ff   header (vsec_id 0, length 0xca)
01=00000013   cable_info (PRESENT|REVERSE|20Gbps) — Linux wrote this
02–0b=0
0c=00000fff
0d=00000fff   12-bit masks, possibly hop/port
0e=0 0f=0
```

Analog timeout `+0x18=00000017` (bit 12 from 0x1017 not latched
this boot). Analog still idle.

## 0059

Start RTKit client endpoints 0x10–0x1f. Probe VSE words 2–11 for
sticky writes; leave +0x02=1 if it sticks (next to cable_info).
