# 0079 skip lpdptxphy instantiate on USB4 skip path

0078 hub-from-init blanked eDP. Skip path still called
`dcp_usb4_enable_lpdptxphy()`, which probes `phy@39c000000`.
`phy_set_mode(DP)` writes `core+0x10` and takes the engine from
the panel.

0079: do not instantiate that PHY. Same 0073 handshake (`0x9001`
unit 0, GET_SUPPORTS_HPD=1, set_hpd before request_display).

Load with hub unplugged; plug after eDP is up.
