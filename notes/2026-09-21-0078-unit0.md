# 0078 stay on RemotePort unit 0

0077 unit 1 DEACTIVATEd. 0073 unit 0 ACTIVATEd. Bind unit 0.

Unattended reboot: `scripts/install-unattended.sh` then
`sudo -n ./scripts/load-appledrm.sh`. Post-boot captures
`notes/last-boot-check.txt` and resumes Grok.
