# 0109: CONNECTED bit set cleanly, monitor still has no signal

Boot55780c37-7c7d-47e4-9e0a-27aaaacd8200. User confirms "connected, nothing
happened" -- same no-signal outcome as0102-0108. eDP stayed connected
throughout. Private captures:0109-right-connected.txt,0109-right-kernel.log,
0109-dpin-counters.txt under captures/ (not committed).

## Sequence

Identical to0105-0108: route selected with gates deferred,native DPIN0
active handshake completes ("native DPIN0: active=1 handshake=0" -- the new
HPD/CONTROL CONNECTED-bit writes ran and returned success, no -EIO/-ENOLINK/
-ETIMEDOUT), SET_LINK_RATE0xa, native crossbar bring-up result0
(000=4,800=4), SET_ACTIVE_LANE_COUNT4, DP IN DPRX_DONE=1, USB4 frame
complete id=2. DP-IN packet counter again nonzero (0x5d34), consistent with
0106-0108.

## What this establishes

The one fully-confirmed, unconditional native register write identified in
notes/2026-09-22-0109-dpin0-connected-bit.md -- CONNECTED (bit1) on DPIN0's
HPD and CONTROL registers, verified by reading the native callee directly,
no runtime-dependent value involved -- is now also ruled out as a sole
explanation. It executed cleanly (handshake result0, no error) and changed
nothing observable about the outcome.

Combined with0106(crossbar/DP-IN emits traffic),0107(traffic reaches the
hub),0108(bandwidth genuinely granted, confirmed via register readback),
and now0109(the one confirmed missing native register write, replicated
exactly), four separate, independently-verified, non-speculative
hypotheses at the USB4/tunnel/DPIN0 level have been tested and ruled out
with hard evidence each time. The remaining candidates identified so far
(the two runtime-dependent DPIN0 fields at+0x14/+0x1c, whose values this
project cannot determine from the static kernelcache alone; the hub's own
internal DP-OUT-to-physical-port firmware; the physical DP-alt-mode link
training between hub and VMM7100) are all either unresolved-by-design
(need runtime data this project cannot capture) or outside anything the
Linux host driver observes or controls.

No speculative register write, kernel change, or reboot performed beyond
what was logged before this attachment. Future0109 boot options remain
disarmed. Unplug requested; see follow-up ACTION-LOG entry once confirmed.
