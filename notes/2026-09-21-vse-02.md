# VSE +0x02 only — 2026-09-21

0060 recovered NHI (`CS0=c0090400`, DPRX=0). Analog `+0x18=0x17`.

0059: VSE +0x02–0x0a write 1 stuck; +0x0b hung config space.
0061 writes **only** +0x02=1 (next to cable_info, stuck safely before
the hang). Do not touch +0x03–0x0b.

If CS after that is still `c0090400`, NHI is fine. DPRX=1 would mean
+0x02 is the analog/AUX enable.
